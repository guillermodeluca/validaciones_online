# ================================== #
# Firebase Project .gitignore        #
# ================================== #

# Node.js specific ignores
# ------------------------
node_modules/
npm-debug.log*
yarn-debug.log
yarn-error.log
.pnp/
.pnp.js
.pnpm-store/

# Environment variables & Local configuration
# ------------------------------------------
# Crucial for sensitive data. Do NOT commit your local .env file.
# We found it was causing issues by accidentally exposing secrets.
.env
.env.local # <-- SUGERENCIA: Añadir si usas .env.local
.env.secret
# Local configuration files for "init functions" (as suggested by Firebase CLI)
# These are used by the Firebase local emulator.
*.local
.runtimeconfig.json # If you're using functions.config(). Don't commit if it holds secrets.
                    # (You've migrated away, so less critical, but still good to ignore if it contains old config)


# Build artifacts & compiled code
# -------------------------------
# Common output directories for TypeScript, JavaScript builds
lib/
dist/
build/
*.tsbuildinfo # TypeScript build info file


# Firebase CLI & Emulators specific ignores
# ----------------------------------------
firebase-debug.log
firestore-debug.log
storage-debug.log
hosting-debug.log
# Catch-all for other log files generated by Firebase CLI
*.log

.firebase/ # Firebase CLI local cache (e.g., login tokens, project aliases cache)
emulators_data/ # Default emulator data directory
nueva_emulators_data/ # Your specific emulator data directory
emulator_data/ # Another specific emulator data directory
mis_datos_emuladores/ # <-- SUGERENCIA: Añadir tu carpeta específica

# Sensitive Firebase credentials
# ------------------------------
# ABSOLUTELY ESSENTIAL: This file contains sensitive Firebase Admin SDK credentials.
# NEVER, EVER, commit this to version control.
serviceAccountKey.json


# IDE and OS specific files
# --------------------------
.DS_Store # macOS specific
.vscode/ # Visual Studio Code settings
*.swp # Vim swap files
Thumbs.db # Windows thumbnail cache

# <-- CRÍTICO: Añadir para ignorar los metadatos de Windows (:Zone.Identifier)
*:Zone.Identifier

# Other temporary or generated files
# ----------------------------------
*.tmp
*.bak
.cache/


# no subir secrets
.env.local
serviceAccountKey.json
